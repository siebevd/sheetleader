version: "3.8"

services:
  frontend-build:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: build
    container_name: sheetleader-frontend-build
    volumes:
      - frontend-dist:/app/dist

  caddy:
    build:
      context: .
      dockerfile: Dockerfile.caddy
    container_name: sheetleader-caddy
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DOMAIN=${DOMAIN:-localhost}
    volumes:
      - frontend-dist:/var/www/html
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - backend
      - frontend-build
      - drizzle-studio
    restart: unless-stopped
    networks:
      - sheetleader-network

  backend:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: sheetleader-backend
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    env_file:
      - ./app/.env
    volumes:
      - ./app/data:/app/data
    restart: unless-stopped
    networks:
      - sheetleader-network

  drizzle-studio:
    build:
      context: ./app
      dockerfile: Dockerfile.studio
    container_name: sheetleader-drizzle-studio
    expose:
      - "4983"
    volumes:
      - ./app:/app
    restart: unless-stopped
    networks:
      - sheetleader-network

networks:
  sheetleader-network:
    driver: bridge

volumes:
  frontend-dist:
  caddy-data:
  caddy-config:

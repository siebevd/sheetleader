# Domain configuration for Caddy
# For local development, use 'localhost' (default)
# For production, use your domain (e.g., 'yourdomain.com' or 'volvermogentegenkanker.byundefined.com')
# When using a real domain, Caddy will automatically obtain SSL certificates from Let's Encrypt
DOMAIN=localhost
